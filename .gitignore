node_modules/
dist/
*.log
.env
sc-loadout-overlay.code-workspace

# local dev/test state
.replay-state/
release/
!packaging/release/
!packaging/release/archverse-doctor
!packaging/release/install-archverse.sh
build/server/

# personal runtime config (erkul URLs + sync token) — seeded from DEFAULTS, stored in %APPDATA%
overlay/config.json

# python bytecode (tools/fab-bgremove)
__pycache__/
*.pyc

# Throwaway profile used by 'npm run dev:fresh' to walk through first-run setup
.dev-profile/
.dev-versefit/

# Sandbox profile used by the widget test harness.
.test-profile/

# Regenerated from the current repository state for chat/session handoff.
/ARCHVERSE-HANDOFF.generated.md
